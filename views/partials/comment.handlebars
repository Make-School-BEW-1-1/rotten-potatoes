<div class="card" id="{{this._id}}">
    <div class="card-block">
        <h4 class="card-title">{{this.title}}</h4>
        <p class="card-text">{{this.content}}</p>

        <p>
            <button class="btn btn-link deleteComment" data-review-id="{{this.reviewId}}" data-comment-id="{{this._id}}">Delete</button>
        </p>
    </div>
</div>
